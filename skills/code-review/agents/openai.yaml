interface:
  display_name: "Review"
  short_description: "Review code and write a severity-ranked markdown report."
  default_prompt: "Use $code-review to inspect this change set and write a severity-ranked markdown review under docs/."
