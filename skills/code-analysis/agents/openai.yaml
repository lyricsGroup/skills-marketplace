interface:
  display_name: "Analysis"
  short_description: "Analyze code paths and write an evidence-backed markdown report."
  default_prompt: "Use $code-analysis to inspect this code path and write an evidence-backed markdown report under docs/."
